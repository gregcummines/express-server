#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../copy-newer/bin/copy-newer-dir" "$@"
  ret=$?
else 
  node  "$basedir/../copy-newer/bin/copy-newer-dir" "$@"
  ret=$?
fi
exit $ret
