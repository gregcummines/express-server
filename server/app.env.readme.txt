The app.env file contains 
-the secret key for JWT authentication. 
-a salt to hash a password (passwords will be saved in the database as a hash)

On linux, run >source app.env

Then if you >printenv

You will see the environment variables get added